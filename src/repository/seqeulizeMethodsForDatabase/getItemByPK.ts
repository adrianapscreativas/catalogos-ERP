import { Model } from "sequelize/types";

/**
 * Function search for Primary key.
 */
 const getItemByPK = async <T extends Model<T>>(model: T, pk:string) => {
    return model.
  };
  
  export default getItemByPK;
  